# Doc Reconciliation

This file was generated by the `/doc-truth-up` documentation-reconciliation pass, which treats
the code as read-only ground truth and edits only documentation so it reflects the repo's actual
current state. No code was changed and no builds or tests were executed.

---

## Per-Claim Findings

### 1. What it is

**Status:** `drifted` (CLAUDE.md only) / `consistent` (README.md)

- **README.md** already correctly states "Moon position via Meeus ELP 2000/82 (truncated), Sun
  position via VSOP87" — verified by `TideEngine/Ephemeris/Ephemeris.swift:3-4` which reads
  `// Moon: Meeus ELP 2000/82 truncated` and `// Sun: VSOP87 truncated`.
- **CLAUDE.md Key Decisions table** said `Pure-Swift VSOP87 truncated` for Ephemeris.
  VSOP87 has no lunar series; the Moon calculation uses Meeus Chapter 47 (ELP 2000/82).
  → **Fixed:** `CLAUDE.md:35` — `Pure-Swift VSOP87 truncated` → `Meeus ELP2000/82 (Moon) + VSOP87 (Sun)`

### 2. Current state

**Status:** `drifted` (CLAUDE.md in two places)

- All four phases are implemented: Phase 0 (ephemeris + Metal shader), Phase 1 (60fps globe
  animation + tap-to-coastline), Phase 2 (NOAA live data + LocalTideView), Phase 3 (Widget +
  StoreKit 2 IAP + WorldTides). This is confirmed by the presence of all source files
  (`TideEngine/Tides/NOAAClient.swift`, `WorldTidesClient.swift`, `TideEngineWidget/TideWidget.swift`,
  `TideEngine/Paywall/StoreManager.swift`, `TideEngine/Paywall/PaywallView.swift`,
  `TideEngineTests/Phase3Tests.swift` with 21 tests) and by `HANDOFF.md:3-16` which states
  "Status: Complete (v1.0 feature-complete)" with 62 tests across three test files.
- **CLAUDE.md** "Current Phase" section said `Phase 0: Foundation + Metal Shader Proof`.
  → **Fixed:** Updated to `Complete — all 4 phases shipped (v1.0)`
- **CLAUDE.md** portfolio-context "Current State" section had the same stale Phase 0 claim.
  → **Fixed:** Updated to `Complete — all 4 phases shipped (v1.0)` with HANDOFF.md reference.

### 3. Stack

**Status:** `consistent` (README.md) / `drifted` (CLAUDE.md — same ephemeris issue as Claim 1)

- **README.md** Tech Stack table lists "Custom Meeus/VSOP87 ephemeris" — correct.
- **CLAUDE.md** Key Decisions row for Ephemeris was wrong (see Claim 1 fix above; same edit covers this).
- XcodeGen listed as a prerequisite in README — confirmed by presence of `project.yml:1`.

### 4. How to run

**Status:** `consistent`

- README Quick Start (`xcodegen generate` + `open TideEngine.xcodeproj`) is verified:
  `project.yml` exists and `TideEngine.xcodeproj/project.pbxproj` exists.
- CLAUDE.md portfolio context `How To Run` ("Build and run the `TideEngine` scheme") is consistent.

### 5. Known risks / constraints

**Status:** `consistent`

- CLAUDE.md "Do NOT" constraints (no Metal for geometry, no WorldTides key in UserDefaults/plist,
  no iPad, no DispatchQueue.main, no API calls from widget) remain accurate and are enforced by
  the code as written (`TideEngine/Globe/GlobeScene.swift`, `TideEngineWidget/TideWidget.swift`,
  `TideEngine/Paywall/KeychainHelper.swift`). The constraint "Do not add features not in the current
  phase" is now a general scope guard rather than a phase gating rule, but remains directionally
  accurate.

### 6. Next move

**Status:** `drifted` (CLAUDE.md portfolio context)

- **CLAUDE.md** portfolio-context "Next Recommended Move" told the reader to "Finish the Phase 0
  foundation and Metal shader proof" — all four phases are already complete.
- `HANDOFF.md:24-28` documents the actual remaining work: physical device testing, replacing the
  placeholder WorldTides API key, and App Store Connect preparation.
  → **Fixed:** Updated to reflect device testing, API key, and App Store Connect steps.

---

## Contradictions for Manual Review

These files are outside the allowed edit set. A human should apply the corrections.

| File | Location | What is wrong | One-line fix |
|------|----------|---------------|--------------|
| `IMPLEMENTATION-ROADMAP.md` | File structure diagram line 64 | Lists `TideWidgetView.swift` as a separate file | Change to note that `TideWidgetView` is defined inside `TideWidget.swift` (no separate file) |
| `IMPLEMENTATION-ROADMAP.md` | Testing Reference table (lines 353–362) | Lists only `EphemerisTests.swift` and `TideAPITests.swift` | Add `Phase3Tests.swift` row: Phase 3 tests covering WorldTides parsing, Keychain, StoreKit state, widget cache, and TidePrediction model |
| `IMPLEMENTATION-ROADMAP.md` | `CachedTideData` struct definition (~line 129) | Missing `stationName: String` and `hourlyCurve: [TideDataPoint]` fields | Add both fields to match `TideEngine/Tides/TideModels.swift:39-46` |
| `CHANGELOG.md` | `[Unreleased]` section | Contains only "Initial release" placeholder with no changelog entries for the completed v1.0 feature set | Add entries for all four shipped phases (ephemeris, globe, NOAA/WorldTides, WidgetKit, StoreKit 2 IAP) |

---

## Footer

Generated: 2026-05-30 21:13:23 PDT  
Branch: `docs/truth-up-2026-05-30`  
HEAD reconciled against: `645015c47ce648cc4ab1092ec80f7b2a49dd643d`
